{% extends 'base.html.twig' %}

{% form_theme form 'General/form_fields.html.twig' %}

{% block content %}
    <div class="panel panel-inverse">
        <div class="panel-heading">
            {#<div class="panel-heading-btn">#}
            {#<a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default" data-click="panel-expand"><i class="fa fa-expand"></i></a>#}
            {#<a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-success" data-click="panel-reload"><i class="fa fa-redo"></i></a>#}
            {#<a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-warning" data-click="panel-collapse"><i class="fa fa-minus"></i></a>#}
            {#<a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-danger" data-click="panel-remove"><i class="fa fa-times"></i></a>#}
            {#</div>#}
            <h4 class="panel-title">Enter Details</h4>
        </div>

        <div class="panel-body">
            {% include 'General/base_disease_header.html.twig' %}

            {{ form_start(form) }}
            {{ form_row(form.under5) }}
            {{ form_row(form.under5With) }}
            {{ form_row(form.suspected) }}
            {{ form_row(form.withFormAndSample) }}

            <div class="row">
                <label class="col-md-12 col-form-label">No. of stool samples positive for rotavirus with forms</label>
            </div>
            <div class="row">
                <div class="col-md-3">
                    {{ form_label(form.positiveUnder12) }}
                    {{ form_widget(form.positiveUnder12) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.positiveUnder23) }}
                    {{ form_widget(form.positiveUnder23) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.positiveUnder59) }}
                    {{ form_widget(form.positiveUnder59) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.positiveTotal) }}
                    {{ form_widget(form.positiveTotal) }}
                </div>
            </div>

            <div class="row">
                <label class="col-md-12 col-form-label">No. of deaths</label>
            </div>
            <div class="row">
                <div class="col-md-3">
                    {{ form_label(form.deathsUnder12) }}
                    {{ form_widget(form.deathsUnder12) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.deathsUnder23) }}
                    {{ form_widget(form.deathsUnder23) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.deathsUnder59) }}
                    {{ form_widget(form.deathsUnder59) }}
                </div>
                <div class="col-md-3">
                    {{ form_label(form.deathsTotal) }}
                    {{ form_widget(form.deathsTotal) }}
                </div>
            </div>

            {{ form_rest(form) }}
            <fieldset>
                {#<legend class="m-b-15">Legend</legend>#}
                <button type="submit" class="btn btn-sm btn-primary m-r-5 pull-right">{{ 'Edit'|trans }}</button>
                <a href="{{ path('rotavirusIndex') }}" class="btn btn-sm btn-default">{{ 'Cancel'|trans }}Cancel</a>
            </fieldset>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
